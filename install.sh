#!/bin/sh

# Change to base directory
cd -P -- "$(dirname -- "$0")"
# printf '%s\n' "$(pwd -P)"

# Make data directory if not present
mkdir -p "data"

# TODO: create virtualenv

# TODO: enter virtualenv

# TODO: install dependencies

# TODO: run initialization
